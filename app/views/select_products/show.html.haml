- title "Select Product"

%p
  %strong Solar Electricity:
  =h @select_product.product_se
%p
  %strong Solar Hot Water:
  =h @select_product.product_shw
%p
  %strong Timeframe:
  =h @select_product.timeframe

%p
  = link_to "Edit", edit_select_product_path(@select_product)
  |
  = link_to "Destroy", @select_product, :confirm => 'Are you sure?', :method => :delete
  |
  = link_to "View All", select_products_path
